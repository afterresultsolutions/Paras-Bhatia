export default function HeroLiveIcon() {
  return (
    <div style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      gap: '0.7em',
      padding: '1em',
      borderRadius: '2em',
      background: 'rgba(0,0,0,0.08)',
      boxShadow: '0 2px 16px rgba(0,0,0,0.05)',
      width: 'clamp(120px, 30vw, 220px)',
      height: 'clamp(40px, 5vw, 80px)',
      position: 'relative',
      animation: 'moveBounce 2s infinite alternate'
    }}>
      <style>
        {`
        @keyframes pulse {
          0% { box-shadow: 0 0 8px 2px #ff0033a0; }
          60% { box-shadow: 0 0 24px 8px #ff0033b0; }
          100% { box-shadow: 0 0 8px 2px #ff0033a0; }
        }
        @keyframes flicker {
          0%,100% { opacity: 1; }
          50% { opacity: 0.7; }
        }
        @keyframes moveBounce {
          0% { transform: translateY(0); }
          50% { transform: translateY(-8px) scale(1.05); }
          100% { transform: translateY(0); }
        }
        `}
      </style>
      <div style={{
        width: 'clamp(16px, 5vw, 32px)',
        height: 'clamp(16px, 5vw, 32px)',
        borderRadius: '50%',
        background: 'radial-gradient(circle at 40% 40%, #ff0033, #ff7c52 80%)',
        boxShadow: '0 0 18px 6px #ff003378',
        animation: 'pulse 1.1s infinite'
      }} />
      <span style={{
        fontFamily: 'Inter, Arial, sans-serif',
        fontWeight: 600,
        fontSize: 'clamp(1rem, 3vw, 2rem)',
        color: '#ff0033',
        letterSpacing: '0.18em',
        textShadow: '0 2px 8px #fff1',
        animation: 'flicker 2s infinite'
      }}>LIVE</span>
    </div>
  );
}
